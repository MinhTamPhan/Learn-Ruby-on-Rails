<div class="form-group">
    <p class="bg-primary text-warning">
      <strong>Title:</strong>
      <%= @article.title %>
    </p>
     
    <p class="bg-primary text-warning">
      <strong>Text:</strong>
      <%= @article.text %>
    </p>
    <%= link_to 'Back', articles_path %>
</div>